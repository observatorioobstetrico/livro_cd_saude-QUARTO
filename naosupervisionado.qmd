## Análise de Agrupamentos

No início do capítulo sobre aprendizado não supervisionado, foi introduzida a análise de agrupamento como uma técnica que visa dividir um conjunto de dados em grupos, também conhecidos como clusters. O objetivo é que os indivíduos dentro do mesmo grupo tenham características semelhantes entre si e distintas dos indivíduos pertencentes a outros grupos. Consideremos um conjunto de 𝑁 observações de um vetor aleatório com 𝑝 variáveis. Cada observação é representada por um vetor $\boldsymbol{x}_i$ com $p$ coordenadas, onde $i$ varia de 1 a $N$. A matriz de dados $\boldsymbol{X}$ pode ser descrita como:

$$
 \begin{align}
  \boldsymbol{X} &= \begin{bmatrix}
           \boldsymbol x_{1} \\
           \boldsymbol x_{2} \\
           \vdots \\
           \boldsymbol x_{p}
         \end{bmatrix} = \begin{bmatrix}
           x_{11} & x_{12} & \cdots & x_{1p}  \\
          x_{21} & x_{22} & \cdots & x_{2p}  \\
            \vdots &\vdots & \ddots &\vdots \\
           x_{N1} & x_{N2} & \cdots & x_{Np}
         \end{bmatrix}.
  \end{align}
$$

Na análise de agrupamento, buscamos identificar regiões no espaço dos dados que possuam um grande número de observações próximas umas das outras. Essas regiões são chamadas de clusters. A ideia é agrupar indivíduos que sejam semelhantes entre si e diferentes dos indivíduos em outros clusters. Essa técnica é chamada de aprendizado não supervisionado, pois não utilizamos uma variável específica como referência para avaliar o resultado do agrupamento.

Formalmente, os clusters são definidos da seguinte forma:

-   Cada cluster é um grupo de observações;
-   Todos os indivíduos pertencem a pelo menos um cluster;
-   Dois clusters diferentes não possuem observações em comum.
Ao realizar o agrupamento de dados, é importante utilizar um método que maximize as diferenças entre os clusters, ao mesmo tempo que minimiza as diferenças dentro de cada cluster. Para isso, são utilizadas medidas de similaridade ou dissimilaridade, que quantificam as diferenças entre as observações.

As medidas de dissimilaridade mais comumente usadas são a distância euclidiana e a distância euclidiana quadrática, como apresentado abaixo respectivamente:

$$
\begin{split}
d(\mathbf{x}_i, \mathbf{x}_i') = \sqrt{\sum_{j=1}^{p} (x_{ij} - x_{i'j})^2}\\
d^2(\mathbf{x}_i, \mathbf{x}_i') = \sum_{j=1}^{p} (x_{ij} - x_{i'j})^2
\end{split}
$$
Outras medidas menos utilizadas incluem a distância absoluta e a distância de Mahalanobis, que leva em consideração a matriz de covariância, respectivamente representadas como:

$$
\begin{split}
d_a(\mathbf{x}_i, \mathbf{x}_i') = \sum_{j=1}^{p} |x_{ij} - x_{i'j}|\\
d_M(\mathbf{x}_i, \mathbf{x}_i') = \sqrt{(\mathbf{x}_i - \mathbf{x}_i')' \mathbf{S}^{-1} (\mathbf{x}_i - \mathbf{x}_i')}
\end{split}
$$

Uma maneira comum de representar as dissimilaridades entre os objetos em um conjunto de dados é por meio de uma matriz de dissimilaridade. Essa matriz mostra os valores de dissimilaridade $a(x_i,x_j)$ entre cada par de objetos $x_i$ e $x_j$ com $i,j = 1,2,\dots,N.$

$$
\begin{align}
A = 
\begin{bmatrix}
          a(x_1,x_1) & a(x_1,x_2) & \cdots &a(x_1,x_N) \\
         a(x_2,x_1) & a(x_2,x_2) & \cdots & a(x_2,x_N)  \\
            \vdots &\vdots & \ddots &\vdots \\
           a(x_N,x_1) & a(x_N,x_2) & \cdots & a(x_N,x_N)
         \end{bmatrix}.
  \end{align}
$$

A análise de agrupamento é uma ferramenta valiosa que oferece uma variedade de utilidades. Uma delas é a capacidade de identificar estratos dentro de uma população em estudo, permitindo que seja realizada uma amostragem que leve em consideração essas subpopulações e, assim, reduzindo o tamanho do conjunto de dados. Além disso, o agrupamento também pode ser usado para detectar dados atípicos, ou outliers, que se comportam de maneira significativamente diferente dos grupos formados.

Existem alguns conceitos importantes que caracterizam os métodos de agrupamento e ajudam a determinar a adequação de cada método. Essas características incluem a escalabilidade, ou seja, a capacidade de lidar eficientemente com grandes conjuntos de dados que podem conter milhões ou até bilhões de observações. Também é relevante considerar a capacidade do método de lidar com diferentes tipos de variáveis, não apenas dados numéricos, mas também dados nominais ou ordinais.

Outro aspecto crucial é a capacidade do método de agrupamento de lidar com clusters de diferentes formatos. Muitos algoritmos de agrupamento são baseados em medidas de distância e, portanto, tendem a encontrar clusters esféricos com tamanho e densidade semelhantes. No entanto, é fundamental avaliar se o método pode identificar grupos com formas mais complexas e não esféricas.

A robustez do método em relação a outliers também é uma consideração importante. Conjuntos de dados reais frequentemente contêm valores discrepantes que podem afetar negativamente a qualidade dos agrupamentos. Portanto, é necessário verificar se o método é capaz de lidar com outliers e não ser excessivamente influenciado por eles.

Além disso, a capacidade de agrupar dados de alta dimensionalidade é um desafio significativo. Com conjuntos de dados contendo muitas variáveis, encontrar clusters relevantes pode se tornar mais difícil. Portanto, é essencial avaliar se o método de agrupamento é eficaz nesse contexto.

Existem diversos métodos de agrupamento descritos na literatura, e pesquisadores continuam a desenvolver técnicas avançadas para superar as limitações dos métodos existentes. É importante ter em mente que diferentes algoritmos podem fornecer resultados diferentes para o mesmo conjunto de dados. Nos próximos capítulos, serão apresentados os métodos de agrupamento considerados neste trabalho, explorando suas vantagens, desvantagens e aplicações adequadas.

## Métodos de Agrupamentos

### Métodos por Particionamento

#### K-médias

#### K-medóides

### Métodos Hierárquicos

## Métodos de Validação
